<!DOCTYPE html>
<html>
<head>
    <title>Products</title>
    <style>
        body {
            font-family: Arial;
        }
        .container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
        }
        .card {
            border: 1px solid #ccc;
            padding: 10px;
            width: 200px;
            text-align: center;
            border-radius: 10px;
        }
        img {
            width: 100%;
            height: 150px;
            object-fit: cover;
        }
    </style>
</head>
<body>

<h1><%=title%></h1>

<div class="container">
    <%for(i=0;i<data.length;i++){%>
    <div class="card">
        <form action="/user/cart/delete/<%=data[i]._id%>" method="post">
            <img src="<%=data[i].imageurl%>" alt="product">
            <h3><%=data[i].name%></h3>
            <p>Category: <%=data[i]['category']%></p>
            <p>Price: $<%=data[i]['price']%></p>
            <button type="submit">Delete</button>
        </form>
    </div>
    <!-- Add more cards manually or dynamically later -->
    <%}%>
</div>

</body>
</html>